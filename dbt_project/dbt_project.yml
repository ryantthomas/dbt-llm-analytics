name: dbt_llm_analytics
version: '1.0.0'
config-version: 2

profile: dbt_llm_analytics

model-paths: ["models"]
seed-paths: ["seeds"]
target-path: "target"
clean-targets: ["target"]

models:
  dbt_llm_analytics:
    staging:
      +materialized: view
    marts:
      +materialized: table
